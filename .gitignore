node_modules/
dist/
.wrangler/
.dev.vars
.env*
*.key.json
public/agentnet.mjs
public/agentnet.sha256
public/version.json
public/agentnet.tar.gz
public/install.sh
admin-home/
main-home/
# `agentnet remove` archives a home to a timestamped sibling. The archive still
# holds the private key, so it must never become an untracked file someone adds.
*-home.archive-*/
smoke.out
